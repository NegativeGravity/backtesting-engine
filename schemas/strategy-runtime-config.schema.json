{
  "$defs": {
    "SchemaVersion": {
      "const": "1.0.0",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "callback_timeout_seconds": {
      "default": 5.0,
      "exclusiveMinimum": 0,
      "maximum": 300,
      "title": "Callback Timeout Seconds",
      "type": "number"
    },
    "fail_on_action_error": {
      "default": true,
      "title": "Fail On Action Error",
      "type": "boolean"
    },
    "history_limit_per_series": {
      "default": 10000,
      "exclusiveMinimum": 0,
      "title": "History Limit Per Series",
      "type": "integer"
    },
    "isolation_mode": {
      "default": "process",
      "enum": [
        "process",
        "in_process"
      ],
      "title": "Isolation Mode",
      "type": "string"
    },
    "max_actions_per_callback": {
      "default": 1000,
      "exclusiveMinimum": 0,
      "title": "Max Actions Per Callback",
      "type": "integer"
    },
    "max_chart_commands_per_callback": {
      "default": 10000,
      "exclusiveMinimum": 0,
      "title": "Max Chart Commands Per Callback",
      "type": "integer"
    },
    "max_feedback_rounds": {
      "default": 4,
      "minimum": 0,
      "title": "Max Feedback Rounds",
      "type": "integer"
    },
    "max_log_records_per_callback": {
      "default": 1000,
      "exclusiveMinimum": 0,
      "title": "Max Log Records Per Callback",
      "type": "integer"
    },
    "schema_version": {
      "$ref": "#/$defs/SchemaVersion",
      "default": "1.0.0"
    },
    "shutdown_timeout_seconds": {
      "default": 5.0,
      "exclusiveMinimum": 0,
      "maximum": 300,
      "title": "Shutdown Timeout Seconds",
      "type": "number"
    },
    "startup_timeout_seconds": {
      "default": 10.0,
      "exclusiveMinimum": 0,
      "maximum": 300,
      "title": "Startup Timeout Seconds",
      "type": "number"
    },
    "warmup_bars_per_series": {
      "default": 500,
      "minimum": 0,
      "title": "Warmup Bars Per Series",
      "type": "integer"
    }
  },
  "title": "StrategyRuntimeConfig",
  "type": "object"
}
